use sound_board::*;

fn main() {
    // let mut sound_board = audio::SoundBoard::init();
    // sound_board.update_hardware_devices().unwrap();
    // sound_board.update_hardware_devices().unwrap();
    AudioMixerApp::start();
}